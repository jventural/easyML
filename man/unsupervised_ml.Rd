% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unsupervised_ml.R
\name{unsupervised_ml}
\alias{unsupervised_ml}
\title{unsupervised_ml: Aprendizaje No Supervisado Automatizado}
\usage{
unsupervised_ml(
  data,
  methods = c("clustering", "reduction", "anomaly"),
  clustering_algorithms = c("kmeans", "hierarchical", "dbscan", "gmm"),
  k = NULL,
  k_range = 2:10,
  distance = c("euclidean", "manhattan", "gower"),
  hclust_method = c("ward.D2", "complete", "average", "single"),
  dbscan_eps = NULL,
  dbscan_minPts = 5,
  gmm_type = "VVV",
  reduction_methods = c("pca", "tsne", "umap"),
  n_components = 2,
  pca_threshold = 0.95,
  tsne_perplexity = 30,
  umap_n_neighbors = 15,
  umap_min_dist = 0.1,
  anomaly_methods = c("isolation_forest", "lof", "mahalanobis"),
  contamination = 0.05,
  lof_k = 20,
  exclude_cols = NULL,
  impute = TRUE,
  impute_method = c("median", "knn", "mean"),
  normalize = TRUE,
  normalize_method = c("zscore", "minmax"),
  treat_outliers = FALSE,
  outlier_percentile = 0.05,
  remove_high_cor = TRUE,
  cor_threshold = 0.9,
  run_eda = TRUE,
  seed = 2024,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame con los datos.}

\item{methods}{Vector con los tipos de analisis a ejecutar:
"clustering", "reduction", "anomaly" (default: todos).}

\item{clustering_algorithms}{Algoritmos de clustering:
"kmeans", "hierarchical", "dbscan", "gmm" (default: todos).}

\item{k}{Numero de clusters. NULL (default) busca automaticamente el optimo.}

\item{k_range}{Rango de k a evaluar cuando k = NULL (default: 2:10).}

\item{distance}{Metrica de distancia: "euclidean", "manhattan", "gower" (default: "euclidean").}

\item{hclust_method}{Metodo de enlace para clustering jerarquico:
"ward.D2", "complete", "average", "single" (default: "ward.D2").}

\item{dbscan_eps}{Radio epsilon para DBSCAN. NULL (default) lo calcula automaticamente.}

\item{dbscan_minPts}{Puntos minimos para DBSCAN (default: 5).}

\item{gmm_type}{Tipo de covarianza para GMM/mclust (default: "VVV").}

\item{reduction_methods}{Metodos de reduccion de dimensionalidad:
"pca", "tsne", "umap" (default: todos).}

\item{n_components}{Numero de dimensiones objetivo para t-SNE y UMAP (default: 2).}

\item{pca_threshold}{Varianza acumulada a conservar con PCA (default: 0.95).}

\item{tsne_perplexity}{Perplejidad para t-SNE (default: 30).}

\item{umap_n_neighbors}{Vecinos para UMAP (default: 15).}

\item{umap_min_dist}{Distancia minima para UMAP (default: 0.1).}

\item{anomaly_methods}{Metodos de deteccion de anomalias:
"isolation_forest", "lof", "mahalanobis" (default: todos).}

\item{contamination}{Proporcion esperada de anomalias (default: 0.05).}

\item{lof_k}{Numero de vecinos para LOF (default: 20).}

\item{exclude_cols}{Nombres de columnas a excluir del analisis (IDs, texto, etc.).}

\item{impute}{Imputar valores faltantes (default: TRUE).}

\item{impute_method}{Metodo de imputacion: "knn", "median", "mean" (default: "median").}

\item{normalize}{Normalizar variables numericas (default: TRUE).}

\item{normalize_method}{Metodo: "zscore", "minmax" (default: "zscore").}

\item{treat_outliers}{Winsorizar outliers antes de analizar (default: FALSE).}

\item{outlier_percentile}{Percentil para winsorizacion (default: 0.05).}

\item{remove_high_cor}{Eliminar variables con alta correlacion (default: TRUE).}

\item{cor_threshold}{Umbral de correlacion (default: 0.90).}

\item{run_eda}{Ejecutar analisis exploratorio (default: TRUE).}

\item{seed}{Semilla para reproducibilidad (default: 2024).}

\item{verbose}{Mostrar progreso en consola (default: TRUE).}
}
\value{
Objeto de clase \code{unsupervisedml} (lista) con componentes:
\describe{
\item{eda}{Resultados del analisis exploratorio}
\item{preprocessed}{Datos preprocesados y parametros de normalizacion}
\item{clustering}{Resultados de clustering: optimal_k, results por algoritmo}
\item{evaluation}{Comparacion de algoritmos: silhouette, Calinski-Harabasz, Davies-Bouldin}
\item{reduction}{Reduccion de dimensionalidad: PCA, t-SNE, UMAP}
\item{anomalies}{Deteccion de anomalias: isolation forest, LOF, Mahalanobis, consenso}
\item{figures}{Lista de graficos ggplot2}
\item{figures_catalog}{Catalogo de figuras con metadatos para reportes}
\item{verbose_text}{Texto completo del verbose capturado}
\item{data_original}{Datos originales}
\item{elapsed_time}{Tiempo de ejecucion en segundos}
}
}
\description{
Funcion principal que ejecuta un pipeline completo de aprendizaje no supervisado.
Automatiza EDA, preprocesamiento, reduccion de dimensionalidad, clustering,
evaluacion de clusters, deteccion de anomalias y visualizacion.
}
\examples{
\dontrun{
# Ejemplo basico con iris (sin Species)
resultado <- unsupervised_ml(
  data = iris[, 1:4]
)

# Solo clustering con k fijo
resultado <- unsupervised_ml(
  data = mis_datos,
  methods = "clustering",
  k = 3
)

# Analisis completo con configuracion
resultado <- unsupervised_ml(
  data = mis_datos,
  exclude_cols = c("id", "nombre"),
  clustering_algorithms = c("kmeans", "hierarchical"),
  k_range = 2:8,
  reduction_methods = c("pca", "umap"),
  anomaly_methods = "lof"
)

# Ver resultados
print(resultado)
summary(resultado)
plot(resultado)
}

}
