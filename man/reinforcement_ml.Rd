% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinforcement_ml.R
\name{reinforcement_ml}
\alias{reinforcement_ml}
\title{reinforcement_ml: Aprendizaje por Refuerzo Automatizado}
\usage{
reinforcement_ml(
  mode = c("bandits", "mdp"),
  env = NULL,
  env_name = NULL,
  grid_size = 4,
  n_states = NULL,
  n_actions = NULL,
  transition_prob = NULL,
  reward_matrix = NULL,
  bandit_data = NULL,
  n_arms = 5,
  arm_probs = NULL,
  context_dim = NULL,
  algorithms = "auto",
  epsilon = 0.1,
  ucb_c = 2,
  thompson_prior = c(1, 1),
  linucb_alpha = 1,
  learning_rate = 0.1,
  discount = 0.99,
  epsilon_decay = 0.995,
  min_epsilon = 0.01,
  n_episodes = 1000,
  n_steps = 100,
  use_python = FALSE,
  python_algo = c("dqn", "ppo"),
  gym_env = "CartPole-v1",
  n_simulations = 1000,
  run_comparison = TRUE,
  seed = 2024,
  verbose = TRUE
)
}
\arguments{
\item{mode}{Modo de operacion: "bandits" o "mdp".}

\item{env}{Entorno custom: lista con funciones $step(state, action) y $reset().}

\item{env_name}{Nombre de entorno incorporado: "gridworld", "cliffwalking", "frozenlake".}

\item{grid_size}{Tamano de grilla para gridworld/frozenlake (default: 4).}

\item{n_states}{Numero de estados para MDPs custom.}

\item{n_actions}{Numero de acciones para MDPs custom.}

\item{transition_prob}{Array P\link{s,a,s'} de probabilidades de transicion.}

\item{reward_matrix}{Matriz R\link{s,a} de recompensas.}

\item{bandit_data}{Data frame historico para bandits offline (columnas: action, reward).}

\item{n_arms}{Numero de brazos para bandits (default: 5).}

\item{arm_probs}{Vector de probabilidades reales por brazo (simulacion).}

\item{context_dim}{Dimension del contexto para bandits contextuales.}

\item{algorithms}{Algoritmos a ejecutar: "auto" o vector con:
Bandits: "epsilon_greedy", "ucb1", "thompson", "linucb", "contextual_ts".
MDP: "qlearning", "sarsa", "value_iteration", "policy_iteration".}

\item{epsilon}{Parametro epsilon para epsilon-greedy (default: 0.1).}

\item{ucb_c}{Parametro de exploracion para UCB1 (default: 2).}

\item{thompson_prior}{Prior Beta(alpha, beta) para Thompson Sampling (default: c(1,1)).}

\item{linucb_alpha}{Parametro alpha para LinUCB (default: 1.0).}

\item{learning_rate}{Tasa de aprendizaje para Q-Learning/SARSA (default: 0.1).}

\item{discount}{Factor de descuento gamma (default: 0.99).}

\item{epsilon_decay}{Factor de decaimiento de epsilon por episodio (default: 0.995).}

\item{min_epsilon}{Epsilon minimo (default: 0.01).}

\item{n_episodes}{Numero de episodios de entrenamiento (default: 1000).}

\item{n_steps}{Pasos maximos por episodio en MDP (default: 100).}

\item{use_python}{Usar metodos Python via reticulate (default: FALSE).}

\item{python_algo}{Algoritmos Python: "dqn", "ppo".}

\item{gym_env}{Nombre del entorno Gymnasium (default: "CartPole-v1").}

\item{n_simulations}{Numero de rondas para bandits (default: 1000).}

\item{run_comparison}{Ejecutar comparacion de algoritmos (default: TRUE).}

\item{seed}{Semilla (default: 2024).}

\item{verbose}{Mostrar progreso (default: TRUE).}
}
\value{
Objeto de clase \code{reinforcementml} (lista) con componentes:
\describe{
\item{params}{Parametros del analisis (mode, algorithms, etc.)}
\item{environment}{Informacion del entorno (name, n_states, n_actions)}
\item{results}{Lista de resultados por algoritmo (policy, reward_history, q_table)}
\item{summary_table}{data.frame comparativo de algoritmos}
\item{best_algorithm}{Nombre del mejor algoritmo}
\item{figures}{Lista de graficos ggplot2}
\item{figures_catalog}{Catalogo de figuras con metadatos}
\item{verbose_text}{Texto completo del verbose capturado}
\item{elapsed_time}{Tiempo de ejecucion en segundos}
}
}
\description{
Pipeline completo de aprendizaje por refuerzo. Soporta dos modos:
bandits (multi-armed y contextuales) y MDP (Q-Learning, SARSA,
Value/Policy Iteration). Incluye entornos incorporados (GridWorld,
CliffWalking, FrozenLake), comparacion de algoritmos y visualizacion.
Opcionalmente integra Python (Stable-Baselines3) para Deep RL.
}
\examples{
\dontrun{
# Bandits basico
result <- reinforcement_ml(
  mode = "bandits",
  n_arms = 10,
  n_simulations = 5000
)
print(result)
plot(result, type = "regret")

# MDP con GridWorld
result_mdp <- reinforcement_ml(
  mode = "mdp",
  env_name = "gridworld",
  n_episodes = 2000
)
plot(result_mdp, type = "qvalues")

# Bandits contextuales
result_ctx <- reinforcement_ml(
  mode = "bandits",
  n_arms = 5,
  context_dim = 3,
  algorithms = c("linucb", "contextual_ts")
)
}

}
